import React from 'react';
import styled from '@emotion/styled';

const HotPinkButton = styled('button')`
  color: hotpink;
`;

export default function() {
  return <HotPinkButton>I should be hot pink!</HotPinkButton>;
}
